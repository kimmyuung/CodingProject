package FunctionPrograming.FunctionPrograming;

public class Function {
    // 명령형 프로그래밍 ( = How to do? )
    // 1. 이메일을 담을 리스트 선언
    // 2. 루프
    // 3. 유저 선언
    // 4. 검증되지 않았는지 체크
    // 5. 않았다면 변수에 이메일 추출
    // 6. 이메일 리스트에 넣기

    // 선언형 프로그래밍 ( = what to do? )
    // 1. 유저리스트에서
        // 1. 검증되지 않은 유저만 골라내서
    // 2. 이메일을 추출해서
    // 3. 리스트로 받기
}
